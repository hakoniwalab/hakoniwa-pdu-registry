

/**
 * ROS message: geometry_msgs/Pose2D
 * Generated by hakoniwa-ros2pdu.
 */
export class Pose2D {
/** @type { number } */
    x;
/** @type { number } */
    y;
/** @type { number } */
    theta;


    constructor() {
        this.x = 0.0;
        this.y = 0.0;
        this.theta = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'x'
            const field_val = this.x;
            if (typeof field_val?.toDict === 'function') {
                d['x'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['x'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['x'] = field_val;
            }
        }
        {
            // handle field 'y'
            const field_val = this.y;
            if (typeof field_val?.toDict === 'function') {
                d['y'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['y'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['y'] = field_val;
            }
        }
        {
            // handle field 'theta'
            const field_val = this.theta;
            if (typeof field_val?.toDict === 'function') {
                d['theta'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['theta'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['theta'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Pose2D }
     */
    static fromDict(d) {
        const obj = new Pose2D();
        if (d.hasOwnProperty('x')) {
            obj.x = d.x;
        }
        if (d.hasOwnProperty('y')) {
            obj.y = d.y;
        }
        if (d.hasOwnProperty('theta')) {
            obj.theta = d.theta;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Pose2D }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
