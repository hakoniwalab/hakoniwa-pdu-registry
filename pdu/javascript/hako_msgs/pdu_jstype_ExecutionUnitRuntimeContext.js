

/**
 * ROS message: hako_msgs/ExecutionUnitRuntimeContext
 * Generated by hakoniwa-ros2pdu.
 */
export class ExecutionUnitRuntimeContext {
/** @type { number } */
    config_hash;
/** @type { number } */
    epoch;
/** @type { number } */
    owner_id;
/** @type { Array<number> } */
    context;


    constructor() {
        this.config_hash = 0;
        this.epoch = 0;
        this.owner_id = 0;
        this.context = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'config_hash'
            const field_val = this.config_hash;
            if (typeof field_val?.toDict === 'function') {
                d['config_hash'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['config_hash'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['config_hash'] = field_val;
            }
        }
        {
            // handle field 'epoch'
            const field_val = this.epoch;
            if (typeof field_val?.toDict === 'function') {
                d['epoch'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['epoch'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['epoch'] = field_val;
            }
        }
        {
            // handle field 'owner_id'
            const field_val = this.owner_id;
            if (typeof field_val?.toDict === 'function') {
                d['owner_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['owner_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['owner_id'] = field_val;
            }
        }
        {
            // handle field 'context'
            const field_val = this.context;
            if (typeof field_val?.toDict === 'function') {
                d['context'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['context'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['context'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ExecutionUnitRuntimeContext }
     */
    static fromDict(d) {
        const obj = new ExecutionUnitRuntimeContext();
        if (d.hasOwnProperty('config_hash')) {
            obj.config_hash = d.config_hash;
        }
        if (d.hasOwnProperty('epoch')) {
            obj.epoch = d.epoch;
        }
        if (d.hasOwnProperty('owner_id')) {
            obj.owner_id = d.owner_id;
        }
        if (d.hasOwnProperty('context')) {
            const item_class = uint8;
            if (Array.isArray(d.context)) {
                obj.context = d.context.map(item => item_class.fromDict(item));
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ExecutionUnitRuntimeContext }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
