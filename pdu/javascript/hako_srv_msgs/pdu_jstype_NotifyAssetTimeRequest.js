import { LogicalTime } from '../hako_srv_msgs/pdu_jstype_LogicalTime.js';


/**
 * ROS message: hako_srv_msgs/NotifyAssetTimeRequest
 * Generated by hakoniwa-ros2pdu.
 */
export class NotifyAssetTimeRequest {
/** @type { bigint } */
    session_id;
/** @type { LogicalTime } */
    asset_time;


    constructor() {
        this.session_id = 0n;
        this.asset_time = new LogicalTime();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'session_id'
            const field_val = this.session_id;
            if (typeof field_val?.toDict === 'function') {
                d['session_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['session_id'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['session_id'] = field_val.toString();
            } else {
                d['session_id'] = field_val;
            }
        }
        {
            // handle field 'asset_time'
            const field_val = this.asset_time;
            if (typeof field_val?.toDict === 'function') {
                d['asset_time'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['asset_time'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['asset_time'] = field_val.toString();
            } else {
                d['asset_time'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { NotifyAssetTimeRequest }
     */
    static fromDict(d) {
        const obj = new NotifyAssetTimeRequest();
        if (d.hasOwnProperty('session_id')) {
            obj.session_id = BigInt(d.session_id);
        }
        if (d.hasOwnProperty('asset_time')) {
            const field_class = LogicalTime;
            if (d.asset_time) {
                obj.asset_time = field_class.fromDict(d.asset_time);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { NotifyAssetTimeRequest }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
