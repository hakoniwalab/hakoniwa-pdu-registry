defmodule HakoPdu.HakoMavlinkMsgs.HakoHilStateQuaternion do
  @moduledoc """
  ROS message: hako_mavlink_msgs/HakoHilStateQuaternion

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    time_usec: 0,

    attitude_quaternion: [],

    rollspeed: 0.0,

    pitchspeed: 0.0,

    yawspeed: 0.0,

    lat: 0,

    lon: 0,

    alt: 0,

    vx: 0,

    vy: 0,

    vz: 0,

    ind_airspeed: 0,

    true_airspeed: 0,

    xacc: 0,

    yacc: 0,

    zacc: 0

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "time_usec" => encode_value(msg.time_usec),

      "attitude_quaternion" => encode_value(msg.attitude_quaternion),

      "rollspeed" => encode_value(msg.rollspeed),

      "pitchspeed" => encode_value(msg.pitchspeed),

      "yawspeed" => encode_value(msg.yawspeed),

      "lat" => encode_value(msg.lat),

      "lon" => encode_value(msg.lon),

      "alt" => encode_value(msg.alt),

      "vx" => encode_value(msg.vx),

      "vy" => encode_value(msg.vy),

      "vz" => encode_value(msg.vz),

      "ind_airspeed" => encode_value(msg.ind_airspeed),

      "true_airspeed" => encode_value(msg.true_airspeed),

      "xacc" => encode_value(msg.xacc),

      "yacc" => encode_value(msg.yacc),

      "zacc" => encode_value(msg.zacc)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      time_usec: map |> get_value(:time_usec, "time_usec", 0) |> decode_time_usec(),

      attitude_quaternion: map |> get_value(:attitude_quaternion, "attitude_quaternion", []) |> decode_attitude_quaternion(),

      rollspeed: map |> get_value(:rollspeed, "rollspeed", 0.0) |> decode_rollspeed(),

      pitchspeed: map |> get_value(:pitchspeed, "pitchspeed", 0.0) |> decode_pitchspeed(),

      yawspeed: map |> get_value(:yawspeed, "yawspeed", 0.0) |> decode_yawspeed(),

      lat: map |> get_value(:lat, "lat", 0) |> decode_lat(),

      lon: map |> get_value(:lon, "lon", 0) |> decode_lon(),

      alt: map |> get_value(:alt, "alt", 0) |> decode_alt(),

      vx: map |> get_value(:vx, "vx", 0) |> decode_vx(),

      vy: map |> get_value(:vy, "vy", 0) |> decode_vy(),

      vz: map |> get_value(:vz, "vz", 0) |> decode_vz(),

      ind_airspeed: map |> get_value(:ind_airspeed, "ind_airspeed", 0) |> decode_ind_airspeed(),

      true_airspeed: map |> get_value(:true_airspeed, "true_airspeed", 0) |> decode_true_airspeed(),

      xacc: map |> get_value(:xacc, "xacc", 0) |> decode_xacc(),

      yacc: map |> get_value(:yacc, "yacc", 0) |> decode_yacc(),

      zacc: map |> get_value(:zacc, "zacc", 0) |> decode_zacc()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_time_usec(value) do
    value
  end


  defp decode_attitude_quaternion(value) do
    value
  end


  defp decode_rollspeed(value) do
    value
  end


  defp decode_pitchspeed(value) do
    value
  end


  defp decode_yawspeed(value) do
    value
  end


  defp decode_lat(value) do
    value
  end


  defp decode_lon(value) do
    value
  end


  defp decode_alt(value) do
    value
  end


  defp decode_vx(value) do
    value
  end


  defp decode_vy(value) do
    value
  end


  defp decode_vz(value) do
    value
  end


  defp decode_ind_airspeed(value) do
    value
  end


  defp decode_true_airspeed(value) do
    value
  end


  defp decode_xacc(value) do
    value
  end


  defp decode_yacc(value) do
    value
  end


  defp decode_zacc(value) do
    value
  end

end
