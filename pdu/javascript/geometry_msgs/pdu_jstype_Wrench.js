import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: geometry_msgs/Wrench
 * Generated by hakoniwa-ros2pdu.
 */
export class Wrench {
/** @type { Vector3 } */
    force;
/** @type { Vector3 } */
    torque;


    constructor() {
        this.force = new Vector3();
        this.torque = new Vector3();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'force'
            const field_val = this.force;
            if (typeof field_val?.toDict === 'function') {
                d['force'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['force'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['force'] = field_val;
            }
        }
        {
            // handle field 'torque'
            const field_val = this.torque;
            if (typeof field_val?.toDict === 'function') {
                d['torque'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['torque'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['torque'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Wrench }
     */
    static fromDict(d) {
        const obj = new Wrench();
        if (d.hasOwnProperty('force')) {
            const field_class = Vector3;
            if (d.force) {
                obj.force = field_class.fromDict(d.force);
            }
        }
        if (d.hasOwnProperty('torque')) {
            const field_class = Vector3;
            if (d.torque) {
                obj.torque = field_class.fromDict(d.torque);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Wrench }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
