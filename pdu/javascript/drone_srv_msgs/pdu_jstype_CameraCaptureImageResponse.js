

/**
 * ROS message: drone_srv_msgs/CameraCaptureImageResponse
 * Generated by hakoniwa-ros2pdu.
 */
export class CameraCaptureImageResponse {
/** @type { boolean } */
    ok;
/** @type { Array<number> } */
    data;
/** @type { string } */
    message;


    constructor() {
        this.ok = false;
        this.data = [];
        this.message = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'ok'
            const field_val = this.ok;
            if (typeof field_val?.toDict === 'function') {
                d['ok'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['ok'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['ok'] = field_val.toString();
            } else {
                d['ok'] = field_val;
            }
        }
        {
            // handle field 'data'
            const field_val = this.data;
            if (typeof field_val?.toDict === 'function') {
                d['data'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['data'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['data'] = field_val.toString();
            } else {
                d['data'] = field_val;
            }
        }
        {
            // handle field 'message'
            const field_val = this.message;
            if (typeof field_val?.toDict === 'function') {
                d['message'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['message'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['message'] = field_val.toString();
            } else {
                d['message'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { CameraCaptureImageResponse }
     */
    static fromDict(d) {
        const obj = new CameraCaptureImageResponse();
        if (d.hasOwnProperty('ok')) {
            obj.ok = d.ok;
        }
        if (d.hasOwnProperty('data')) {
            obj.data = d.data;
        }
        if (d.hasOwnProperty('message')) {
            obj.message = d.message;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { CameraCaptureImageResponse }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
