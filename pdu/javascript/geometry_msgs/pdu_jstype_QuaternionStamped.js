import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Quaternion } from '../geometry_msgs/pdu_jstype_Quaternion.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: geometry_msgs/QuaternionStamped
 * Generated by hakoniwa-ros2pdu.
 */
export class QuaternionStamped {
/** @type { Header } */
    header;
/** @type { Quaternion } */
    quaternion;


    constructor() {
        this.header = new Header();
        this.quaternion = new Quaternion();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'quaternion'
            const field_val = this.quaternion;
            if (typeof field_val?.toDict === 'function') {
                d['quaternion'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['quaternion'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['quaternion'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { QuaternionStamped }
     */
    static fromDict(d) {
        const obj = new QuaternionStamped();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('quaternion')) {
            const field_class = Quaternion;
            if (d.quaternion) {
                obj.quaternion = field_class.fromDict(d.quaternion);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { QuaternionStamped }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
