import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: sensor_msgs/TimeReference
 * Generated by hakoniwa-ros2pdu.
 */
export class TimeReference {
/** @type { Header } */
    header;
/** @type { Time } */
    time_ref;
/** @type { string } */
    source;


    constructor() {
        this.header = new Header();
        this.time_ref = new Time();
        this.source = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'time_ref'
            const field_val = this.time_ref;
            if (typeof field_val?.toDict === 'function') {
                d['time_ref'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['time_ref'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['time_ref'] = field_val;
            }
        }
        {
            // handle field 'source'
            const field_val = this.source;
            if (typeof field_val?.toDict === 'function') {
                d['source'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['source'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['source'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { TimeReference }
     */
    static fromDict(d) {
        const obj = new TimeReference();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('time_ref')) {
            const field_class = Time;
            if (d.time_ref) {
                obj.time_ref = field_class.fromDict(d.time_ref);
            }
        }
        if (d.hasOwnProperty('source')) {
            obj.source = d.source;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { TimeReference }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
