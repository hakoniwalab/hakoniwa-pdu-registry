import { Duration } from '../builtin_interfaces/pdu_jstype_Duration.js';


/**
 * ROS message: trajectory_msgs/JointTrajectoryPoint
 * Generated by hakoniwa-ros2pdu.
 */
export class JointTrajectoryPoint {
/** @type { Array<number> } */
    positions;
/** @type { Array<number> } */
    velocities;
/** @type { Array<number> } */
    accelerations;
/** @type { Array<number> } */
    effort;
/** @type { Duration } */
    time_from_start;


    constructor() {
        this.positions = [];
        this.velocities = [];
        this.accelerations = [];
        this.effort = [];
        this.time_from_start = new Duration();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'positions'
            const field_val = this.positions;
            if (typeof field_val?.toDict === 'function') {
                d['positions'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['positions'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['positions'] = field_val.toString();
            } else {
                d['positions'] = field_val;
            }
        }
        {
            // handle field 'velocities'
            const field_val = this.velocities;
            if (typeof field_val?.toDict === 'function') {
                d['velocities'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['velocities'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['velocities'] = field_val.toString();
            } else {
                d['velocities'] = field_val;
            }
        }
        {
            // handle field 'accelerations'
            const field_val = this.accelerations;
            if (typeof field_val?.toDict === 'function') {
                d['accelerations'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['accelerations'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['accelerations'] = field_val.toString();
            } else {
                d['accelerations'] = field_val;
            }
        }
        {
            // handle field 'effort'
            const field_val = this.effort;
            if (typeof field_val?.toDict === 'function') {
                d['effort'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['effort'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['effort'] = field_val.toString();
            } else {
                d['effort'] = field_val;
            }
        }
        {
            // handle field 'time_from_start'
            const field_val = this.time_from_start;
            if (typeof field_val?.toDict === 'function') {
                d['time_from_start'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['time_from_start'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['time_from_start'] = field_val.toString();
            } else {
                d['time_from_start'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { JointTrajectoryPoint }
     */
    static fromDict(d) {
        const obj = new JointTrajectoryPoint();
        if (d.hasOwnProperty('positions')) {
            obj.positions = d.positions;
        }
        if (d.hasOwnProperty('velocities')) {
            obj.velocities = d.velocities;
        }
        if (d.hasOwnProperty('accelerations')) {
            obj.accelerations = d.accelerations;
        }
        if (d.hasOwnProperty('effort')) {
            obj.effort = d.effort;
        }
        if (d.hasOwnProperty('time_from_start')) {
            const field_class = Duration;
            if (d.time_from_start) {
                obj.time_from_start = field_class.fromDict(d.time_from_start);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { JointTrajectoryPoint }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
