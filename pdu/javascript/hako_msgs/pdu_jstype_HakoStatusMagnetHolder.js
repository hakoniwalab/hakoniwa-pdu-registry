

/**
 * ROS message: hako_msgs/HakoStatusMagnetHolder
 * Generated by hakoniwa-ros2pdu.
 */
export class HakoStatusMagnetHolder {
/** @type { boolean } */
    magnet_on;
/** @type { boolean } */
    contact_on;


    constructor() {
        this.magnet_on = false;
        this.contact_on = false;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'magnet_on'
            const field_val = this.magnet_on;
            if (typeof field_val?.toDict === 'function') {
                d['magnet_on'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['magnet_on'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['magnet_on'] = field_val.toString();
            } else {
                d['magnet_on'] = field_val;
            }
        }
        {
            // handle field 'contact_on'
            const field_val = this.contact_on;
            if (typeof field_val?.toDict === 'function') {
                d['contact_on'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['contact_on'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['contact_on'] = field_val.toString();
            } else {
                d['contact_on'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { HakoStatusMagnetHolder }
     */
    static fromDict(d) {
        const obj = new HakoStatusMagnetHolder();
        if (d.hasOwnProperty('magnet_on')) {
            obj.magnet_on = d.magnet_on;
        }
        if (d.hasOwnProperty('contact_on')) {
            obj.contact_on = d.contact_on;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { HakoStatusMagnetHolder }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
