import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: sensor_msgs/Range
 * Generated by hakoniwa-ros2pdu.
 */
export class Range {
/** @type { Header } */
    header;
/** @type { number } */
    radiation_type;
/** @type { number } */
    field_of_view;
/** @type { number } */
    min_range;
/** @type { number } */
    max_range;
/** @type { number } */
    range;


    constructor() {
        this.header = new Header();
        this.radiation_type = 0;
        this.field_of_view = 0.0;
        this.min_range = 0.0;
        this.max_range = 0.0;
        this.range = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'radiation_type'
            const field_val = this.radiation_type;
            if (typeof field_val?.toDict === 'function') {
                d['radiation_type'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['radiation_type'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['radiation_type'] = field_val;
            }
        }
        {
            // handle field 'field_of_view'
            const field_val = this.field_of_view;
            if (typeof field_val?.toDict === 'function') {
                d['field_of_view'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['field_of_view'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['field_of_view'] = field_val;
            }
        }
        {
            // handle field 'min_range'
            const field_val = this.min_range;
            if (typeof field_val?.toDict === 'function') {
                d['min_range'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['min_range'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['min_range'] = field_val;
            }
        }
        {
            // handle field 'max_range'
            const field_val = this.max_range;
            if (typeof field_val?.toDict === 'function') {
                d['max_range'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['max_range'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['max_range'] = field_val;
            }
        }
        {
            // handle field 'range'
            const field_val = this.range;
            if (typeof field_val?.toDict === 'function') {
                d['range'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['range'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['range'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Range }
     */
    static fromDict(d) {
        const obj = new Range();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('radiation_type')) {
            obj.radiation_type = d.radiation_type;
        }
        if (d.hasOwnProperty('field_of_view')) {
            obj.field_of_view = d.field_of_view;
        }
        if (d.hasOwnProperty('min_range')) {
            obj.min_range = d.min_range;
        }
        if (d.hasOwnProperty('max_range')) {
            obj.max_range = d.max_range;
        }
        if (d.hasOwnProperty('range')) {
            obj.range = d.range;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Range }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
