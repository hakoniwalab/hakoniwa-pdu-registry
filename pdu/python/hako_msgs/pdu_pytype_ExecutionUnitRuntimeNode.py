from typing import List

# dependencies for the generated Python class


# class definition for the ROS message
class ExecutionUnitRuntimeNode:
    """
    ROS message: hako_msgs/ExecutionUnitRuntimeNode
    Generated by hakoniwa-ros2pdu.
    """

    node_id: int

    def __init__(self):
        self.node_id = 0

    def __str__(self):
        return f"ExecutionUnitRuntimeNode(" + ", ".join([
            f"node_id={self.node_id}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()

    def to_dict(self):
        """
        Convert the object to a dictionary.
        """
        d = {}
        # handle field 'node_id'
        field_val = self.node_id
        if isinstance(field_val, bytearray):
            d['node_id'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['node_id'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['node_id'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['node_id'] = field_val
        return d

    @classmethod
    def from_dict(cls, d: dict):
        """
        Create an object from a dictionary.
        """
        obj = cls()
        # handle field 'node_id'
        if 'node_id' in d:
            field_type = cls.__annotations__.get('node_id')
            value = d['node_id']
            
            if field_type is bytearray:
                obj.node_id = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.node_id = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.node_id = value
            elif hasattr(field_type, 'from_dict'):
                obj.node_id = field_type.from_dict(value)
            else:
                obj.node_id = value
        return obj

    def to_json(self, indent=2):
        """
        Convert the object to a JSON string.
        """
        import json
        return json.dumps(self.to_dict(), indent=indent)

    @classmethod
    def from_json(cls, json_str: str):
        """
        Create an object from a JSON string.
        """
        import json
        return cls.from_dict(json.loads(json_str))
