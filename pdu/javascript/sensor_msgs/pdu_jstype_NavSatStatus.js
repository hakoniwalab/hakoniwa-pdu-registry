

/**
 * ROS message: sensor_msgs/NavSatStatus
 * Generated by hakoniwa-ros2pdu.
 */
export class NavSatStatus {
/** @type { number } */
    status;
/** @type { number } */
    service;


    constructor() {
        this.status = 0;
        this.service = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'status'
            const field_val = this.status;
            if (typeof field_val?.toDict === 'function') {
                d['status'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['status'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['status'] = field_val;
            }
        }
        {
            // handle field 'service'
            const field_val = this.service;
            if (typeof field_val?.toDict === 'function') {
                d['service'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['service'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['service'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { NavSatStatus }
     */
    static fromDict(d) {
        const obj = new NavSatStatus();
        if (d.hasOwnProperty('status')) {
            obj.status = d.status;
        }
        if (d.hasOwnProperty('service')) {
            obj.service = d.service;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { NavSatStatus }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
