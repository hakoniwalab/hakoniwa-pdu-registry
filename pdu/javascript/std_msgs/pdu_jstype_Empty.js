

/**
 * ROS message: std_msgs/Empty
 * Generated by hakoniwa-ros2pdu.
 */
export class Empty {
/** @type { number } */
    hako_dummy;


    constructor() {
        this.hako_dummy = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'hako_dummy'
            const field_val = this.hako_dummy;
            if (typeof field_val?.toDict === 'function') {
                d['hako_dummy'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['hako_dummy'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['hako_dummy'] = field_val.toString();
            } else {
                d['hako_dummy'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Empty }
     */
    static fromDict(d) {
        const obj = new Empty();
        if (d.hasOwnProperty('hako_dummy')) {
            obj.hako_dummy = d.hako_dummy;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Empty }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
