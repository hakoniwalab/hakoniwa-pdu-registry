import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: sensor_msgs/RelativeHumidity
 * Generated by hakoniwa-ros2pdu.
 */
export class RelativeHumidity {
/** @type { Header } */
    header;
/** @type { number } */
    relative_humidity;
/** @type { number } */
    variance;


    constructor() {
        this.header = new Header();
        this.relative_humidity = 0.0;
        this.variance = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'relative_humidity'
            const field_val = this.relative_humidity;
            if (typeof field_val?.toDict === 'function') {
                d['relative_humidity'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['relative_humidity'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['relative_humidity'] = field_val;
            }
        }
        {
            // handle field 'variance'
            const field_val = this.variance;
            if (typeof field_val?.toDict === 'function') {
                d['variance'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['variance'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['variance'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { RelativeHumidity }
     */
    static fromDict(d) {
        const obj = new RelativeHumidity();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('relative_humidity')) {
            obj.relative_humidity = d.relative_humidity;
        }
        if (d.hasOwnProperty('variance')) {
            obj.variance = d.variance;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { RelativeHumidity }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
