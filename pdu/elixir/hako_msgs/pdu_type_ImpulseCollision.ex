defmodule HakoPdu.HakoMsgs.ImpulseCollision do
  @moduledoc """
  ROS message: hako_msgs/ImpulseCollision

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    collision: false,

    is_target_static: false,

    restitution_coefficient: 0.0,

    self_contact_vector: HakoPdu.GeometryMsgs.Point.new(),

    normal: HakoPdu.GeometryMsgs.Vector3.new(),

    target_contact_vector: HakoPdu.GeometryMsgs.Point.new(),

    target_velocity: HakoPdu.GeometryMsgs.Vector3.new(),

    target_angular_velocity: HakoPdu.GeometryMsgs.Vector3.new(),

    target_euler: HakoPdu.GeometryMsgs.Vector3.new(),

    target_inertia: HakoPdu.GeometryMsgs.Vector3.new(),

    target_mass: 0.0

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "collision" => encode_value(msg.collision),

      "is_target_static" => encode_value(msg.is_target_static),

      "restitution_coefficient" => encode_value(msg.restitution_coefficient),

      "self_contact_vector" => encode_value(msg.self_contact_vector),

      "normal" => encode_value(msg.normal),

      "target_contact_vector" => encode_value(msg.target_contact_vector),

      "target_velocity" => encode_value(msg.target_velocity),

      "target_angular_velocity" => encode_value(msg.target_angular_velocity),

      "target_euler" => encode_value(msg.target_euler),

      "target_inertia" => encode_value(msg.target_inertia),

      "target_mass" => encode_value(msg.target_mass)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      collision: map |> get_value(:collision, "collision", false) |> decode_collision(),

      is_target_static: map |> get_value(:is_target_static, "is_target_static", false) |> decode_is_target_static(),

      restitution_coefficient: map |> get_value(:restitution_coefficient, "restitution_coefficient", 0.0) |> decode_restitution_coefficient(),

      self_contact_vector: map |> get_value(:self_contact_vector, "self_contact_vector", HakoPdu.GeometryMsgs.Point.new()) |> decode_self_contact_vector(),

      normal: map |> get_value(:normal, "normal", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_normal(),

      target_contact_vector: map |> get_value(:target_contact_vector, "target_contact_vector", HakoPdu.GeometryMsgs.Point.new()) |> decode_target_contact_vector(),

      target_velocity: map |> get_value(:target_velocity, "target_velocity", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_target_velocity(),

      target_angular_velocity: map |> get_value(:target_angular_velocity, "target_angular_velocity", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_target_angular_velocity(),

      target_euler: map |> get_value(:target_euler, "target_euler", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_target_euler(),

      target_inertia: map |> get_value(:target_inertia, "target_inertia", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_target_inertia(),

      target_mass: map |> get_value(:target_mass, "target_mass", 0.0) |> decode_target_mass()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_collision(value) do
    value
  end


  defp decode_is_target_static(value) do
    value
  end


  defp decode_restitution_coefficient(value) do
    value
  end


  defp decode_self_contact_vector(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Point.from_map(value), else: value
  end


  defp decode_normal(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_target_contact_vector(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Point.from_map(value), else: value
  end


  defp decode_target_velocity(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_target_angular_velocity(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_target_euler(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_target_inertia(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_target_mass(value) do
    value
  end

end
