defmodule HakoPdu.DroneSrvMsgs.DroneGoToRequest do
  @moduledoc """
  ROS message: drone_srv_msgs/DroneGoToRequest

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    drone_name: "",

    target_pose: HakoPdu.GeometryMsgs.Vector3.new(),

    speed_m_s: 0.0,

    yaw_deg: 0.0,

    tolerance_m: 0.0,

    timeout_sec: 0.0

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "drone_name" => encode_value(msg.drone_name),

      "target_pose" => encode_value(msg.target_pose),

      "speed_m_s" => encode_value(msg.speed_m_s),

      "yaw_deg" => encode_value(msg.yaw_deg),

      "tolerance_m" => encode_value(msg.tolerance_m),

      "timeout_sec" => encode_value(msg.timeout_sec)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      drone_name: map |> get_value(:drone_name, "drone_name", "") |> decode_drone_name(),

      target_pose: map |> get_value(:target_pose, "target_pose", HakoPdu.GeometryMsgs.Vector3.new()) |> decode_target_pose(),

      speed_m_s: map |> get_value(:speed_m_s, "speed_m_s", 0.0) |> decode_speed_m_s(),

      yaw_deg: map |> get_value(:yaw_deg, "yaw_deg", 0.0) |> decode_yaw_deg(),

      tolerance_m: map |> get_value(:tolerance_m, "tolerance_m", 0.0) |> decode_tolerance_m(),

      timeout_sec: map |> get_value(:timeout_sec, "timeout_sec", 0.0) |> decode_timeout_sec()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_drone_name(value) do
    value
  end


  defp decode_target_pose(value) do
    if is_map(value), do: HakoPdu.GeometryMsgs.Vector3.from_map(value), else: value
  end


  defp decode_speed_m_s(value) do
    value
  end


  defp decode_yaw_deg(value) do
    value
  end


  defp decode_tolerance_m(value) do
    value
  end


  defp decode_timeout_sec(value) do
    value
  end

end
