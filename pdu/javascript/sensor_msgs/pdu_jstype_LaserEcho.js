

/**
 * ROS message: sensor_msgs/LaserEcho
 * Generated by hakoniwa-ros2pdu.
 */
export class LaserEcho {
/** @type { Array<number> } */
    echoes;


    constructor() {
        this.echoes = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'echoes'
            const field_val = this.echoes;
            if (typeof field_val?.toDict === 'function') {
                d['echoes'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['echoes'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['echoes'] = field_val.toString();
            } else {
                d['echoes'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { LaserEcho }
     */
    static fromDict(d) {
        const obj = new LaserEcho();
        if (d.hasOwnProperty('echoes')) {
            obj.echoes = d.echoes;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { LaserEcho }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
