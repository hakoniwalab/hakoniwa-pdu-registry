from typing import List

# dependencies for the generated Python class


# class definition for the ROS message
class AckermannDrive:
    """
    ROS message: ackermann_msgs/AckermannDrive
    Generated by hakoniwa-ros2pdu.
    """

    steering_angle: float
    steering_angle_velocity: float
    speed: float
    acceleration: float
    jerk: float

    def __init__(self):
        self.steering_angle = 0.0
        self.steering_angle_velocity = 0.0
        self.speed = 0.0
        self.acceleration = 0.0
        self.jerk = 0.0

    def __str__(self):
        return f"AckermannDrive(" + ", ".join([
            f"steering_angle={self.steering_angle}"
            f"steering_angle_velocity={self.steering_angle_velocity}"
            f"speed={self.speed}"
            f"acceleration={self.acceleration}"
            f"jerk={self.jerk}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()

    def to_dict(self):
        """
        Convert the object to a dictionary.
        """
        d = {}
        # handle field 'steering_angle'
        field_val = self.steering_angle
        if isinstance(field_val, bytearray):
            d['steering_angle'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['steering_angle'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['steering_angle'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['steering_angle'] = field_val
        # handle field 'steering_angle_velocity'
        field_val = self.steering_angle_velocity
        if isinstance(field_val, bytearray):
            d['steering_angle_velocity'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['steering_angle_velocity'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['steering_angle_velocity'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['steering_angle_velocity'] = field_val
        # handle field 'speed'
        field_val = self.speed
        if isinstance(field_val, bytearray):
            d['speed'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['speed'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['speed'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['speed'] = field_val
        # handle field 'acceleration'
        field_val = self.acceleration
        if isinstance(field_val, bytearray):
            d['acceleration'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['acceleration'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['acceleration'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['acceleration'] = field_val
        # handle field 'jerk'
        field_val = self.jerk
        if isinstance(field_val, bytearray):
            d['jerk'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['jerk'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['jerk'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['jerk'] = field_val
        return d

    @classmethod
    def from_dict(cls, d: dict):
        """
        Create an object from a dictionary.
        """
        obj = cls()
        # handle field 'steering_angle'
        if 'steering_angle' in d:
            field_type = cls.__annotations__.get('steering_angle')
            value = d['steering_angle']

            if field_type is bytearray:
                obj.steering_angle = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.steering_angle = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.steering_angle = value
            elif hasattr(field_type, 'from_dict'):
                obj.steering_angle = field_type.from_dict(value)
            else:
                obj.steering_angle = value
        # handle field 'steering_angle_velocity'
        if 'steering_angle_velocity' in d:
            field_type = cls.__annotations__.get('steering_angle_velocity')
            value = d['steering_angle_velocity']

            if field_type is bytearray:
                obj.steering_angle_velocity = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.steering_angle_velocity = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.steering_angle_velocity = value
            elif hasattr(field_type, 'from_dict'):
                obj.steering_angle_velocity = field_type.from_dict(value)
            else:
                obj.steering_angle_velocity = value
        # handle field 'speed'
        if 'speed' in d:
            field_type = cls.__annotations__.get('speed')
            value = d['speed']

            if field_type is bytearray:
                obj.speed = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.speed = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.speed = value
            elif hasattr(field_type, 'from_dict'):
                obj.speed = field_type.from_dict(value)
            else:
                obj.speed = value
        # handle field 'acceleration'
        if 'acceleration' in d:
            field_type = cls.__annotations__.get('acceleration')
            value = d['acceleration']

            if field_type is bytearray:
                obj.acceleration = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.acceleration = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.acceleration = value
            elif hasattr(field_type, 'from_dict'):
                obj.acceleration = field_type.from_dict(value)
            else:
                obj.acceleration = value
        # handle field 'jerk'
        if 'jerk' in d:
            field_type = cls.__annotations__.get('jerk')
            value = d['jerk']

            if field_type is bytearray:
                obj.jerk = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.jerk = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.jerk = value
            elif hasattr(field_type, 'from_dict'):
                obj.jerk = field_type.from_dict(value)
            else:
                obj.jerk = value
        return obj

    def to_json(self, indent=2):
        """
        Convert the object to a JSON string.
        """
        import json
        return json.dumps(self.to_dict(), indent=indent)

    @classmethod
    def from_json(cls, json_str: str):
        """
        Create an object from a JSON string.
        """
        import json
        return cls.from_dict(json.loads(json_str))
