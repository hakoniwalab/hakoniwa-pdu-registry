

/**
 * ROS message: hako_msgs/ExecutionUnitRuntimeStatus
 * Generated by hakoniwa-ros2pdu.
 */
export class ExecutionUnitRuntimeStatus {
/** @type { number } */
    config_hash;
/** @type { number } */
    unit_count;
/** @type { Array<number> } */
    status;
/** @type { Array<number> } */
    epoch;
/** @type { Array<number> } */
    curr_owner_node_id;
/** @type { Array<number> } */
    next_owner_node_id;


    constructor() {
        this.config_hash = 0;
        this.unit_count = 0;
        this.status = [];
        this.epoch = [];
        this.curr_owner_node_id = [];
        this.next_owner_node_id = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'config_hash'
            const field_val = this.config_hash;
            if (typeof field_val?.toDict === 'function') {
                d['config_hash'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['config_hash'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['config_hash'] = field_val;
            }
        }
        {
            // handle field 'unit_count'
            const field_val = this.unit_count;
            if (typeof field_val?.toDict === 'function') {
                d['unit_count'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['unit_count'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['unit_count'] = field_val;
            }
        }
        {
            // handle field 'status'
            const field_val = this.status;
            if (typeof field_val?.toDict === 'function') {
                d['status'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['status'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['status'] = field_val;
            }
        }
        {
            // handle field 'epoch'
            const field_val = this.epoch;
            if (typeof field_val?.toDict === 'function') {
                d['epoch'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['epoch'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['epoch'] = field_val;
            }
        }
        {
            // handle field 'curr_owner_node_id'
            const field_val = this.curr_owner_node_id;
            if (typeof field_val?.toDict === 'function') {
                d['curr_owner_node_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['curr_owner_node_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['curr_owner_node_id'] = field_val;
            }
        }
        {
            // handle field 'next_owner_node_id'
            const field_val = this.next_owner_node_id;
            if (typeof field_val?.toDict === 'function') {
                d['next_owner_node_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['next_owner_node_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['next_owner_node_id'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ExecutionUnitRuntimeStatus }
     */
    static fromDict(d) {
        const obj = new ExecutionUnitRuntimeStatus();
        if (d.hasOwnProperty('config_hash')) {
            obj.config_hash = d.config_hash;
        }
        if (d.hasOwnProperty('unit_count')) {
            obj.unit_count = d.unit_count;
        }
        if (d.hasOwnProperty('status')) {
            const item_class = uint8;
            if (Array.isArray(d.status)) {
                obj.status = d.status.map(item => item_class.fromDict(item));
            }
        }
        if (d.hasOwnProperty('epoch')) {
            const item_class = uint8;
            if (Array.isArray(d.epoch)) {
                obj.epoch = d.epoch.map(item => item_class.fromDict(item));
            }
        }
        if (d.hasOwnProperty('curr_owner_node_id')) {
            const item_class = uint8;
            if (Array.isArray(d.curr_owner_node_id)) {
                obj.curr_owner_node_id = d.curr_owner_node_id.map(item => item_class.fromDict(item));
            }
        }
        if (d.hasOwnProperty('next_owner_node_id')) {
            const item_class = uint8;
            if (Array.isArray(d.next_owner_node_id)) {
                obj.next_owner_node_id = d.next_owner_node_id.map(item => item_class.fromDict(item));
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ExecutionUnitRuntimeStatus }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
