

/**
 * ROS message: hako_msgs/DroneVisualState
 * Generated by hakoniwa-ros2pdu.
 */
export class DroneVisualState {
/** @type { number } */
    x;
/** @type { number } */
    y;
/** @type { number } */
    z;
/** @type { number } */
    roll;
/** @type { number } */
    pitch;
/** @type { number } */
    yaw;
/** @type { Array<number> } */
    pwm_duty;


    constructor() {
        this.x = 0.0;
        this.y = 0.0;
        this.z = 0.0;
        this.roll = 0.0;
        this.pitch = 0.0;
        this.yaw = 0.0;
        this.pwm_duty = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'x'
            const field_val = this.x;
            if (typeof field_val?.toDict === 'function') {
                d['x'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['x'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['x'] = field_val;
            }
        }
        {
            // handle field 'y'
            const field_val = this.y;
            if (typeof field_val?.toDict === 'function') {
                d['y'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['y'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['y'] = field_val;
            }
        }
        {
            // handle field 'z'
            const field_val = this.z;
            if (typeof field_val?.toDict === 'function') {
                d['z'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['z'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['z'] = field_val;
            }
        }
        {
            // handle field 'roll'
            const field_val = this.roll;
            if (typeof field_val?.toDict === 'function') {
                d['roll'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['roll'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['roll'] = field_val;
            }
        }
        {
            // handle field 'pitch'
            const field_val = this.pitch;
            if (typeof field_val?.toDict === 'function') {
                d['pitch'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['pitch'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['pitch'] = field_val;
            }
        }
        {
            // handle field 'yaw'
            const field_val = this.yaw;
            if (typeof field_val?.toDict === 'function') {
                d['yaw'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['yaw'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['yaw'] = field_val;
            }
        }
        {
            // handle field 'pwm_duty'
            const field_val = this.pwm_duty;
            if (typeof field_val?.toDict === 'function') {
                d['pwm_duty'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['pwm_duty'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['pwm_duty'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DroneVisualState }
     */
    static fromDict(d) {
        const obj = new DroneVisualState();
        if (d.hasOwnProperty('x')) {
            obj.x = d.x;
        }
        if (d.hasOwnProperty('y')) {
            obj.y = d.y;
        }
        if (d.hasOwnProperty('z')) {
            obj.z = d.z;
        }
        if (d.hasOwnProperty('roll')) {
            obj.roll = d.roll;
        }
        if (d.hasOwnProperty('pitch')) {
            obj.pitch = d.pitch;
        }
        if (d.hasOwnProperty('yaw')) {
            obj.yaw = d.yaw;
        }
        if (d.hasOwnProperty('pwm_duty')) {
            obj.pwm_duty = d.pwm_duty;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DroneVisualState }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
