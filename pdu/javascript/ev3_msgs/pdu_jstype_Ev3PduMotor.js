

/**
 * ROS message: ev3_msgs/Ev3PduMotor
 * Generated by hakoniwa-ros2pdu.
 */
export class Ev3PduMotor {
/** @type { number } */
    power;
/** @type { number } */
    stop;
/** @type { number } */
    reset_angle;


    constructor() {
        this.power = 0;
        this.stop = 0;
        this.reset_angle = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'power'
            const field_val = this.power;
            if (typeof field_val?.toDict === 'function') {
                d['power'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['power'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['power'] = field_val.toString();
            } else {
                d['power'] = field_val;
            }
        }
        {
            // handle field 'stop'
            const field_val = this.stop;
            if (typeof field_val?.toDict === 'function') {
                d['stop'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['stop'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['stop'] = field_val.toString();
            } else {
                d['stop'] = field_val;
            }
        }
        {
            // handle field 'reset_angle'
            const field_val = this.reset_angle;
            if (typeof field_val?.toDict === 'function') {
                d['reset_angle'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['reset_angle'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['reset_angle'] = field_val.toString();
            } else {
                d['reset_angle'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Ev3PduMotor }
     */
    static fromDict(d) {
        const obj = new Ev3PduMotor();
        if (d.hasOwnProperty('power')) {
            obj.power = d.power;
        }
        if (d.hasOwnProperty('stop')) {
            obj.stop = d.stop;
        }
        if (d.hasOwnProperty('reset_angle')) {
            obj.reset_angle = d.reset_angle;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Ev3PduMotor }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
