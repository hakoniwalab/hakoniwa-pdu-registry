from typing import List

# dependencies for the generated Python class
from ..sensor_msgs.pdu_pytype_ChannelFloat32 import ChannelFloat32
from ..std_msgs.pdu_pytype_Header import Header
from ..geometry_msgs.pdu_pytype_Point32 import Point32
from ..builtin_interfaces.pdu_pytype_Time import Time


# class definition for the ROS message
class PointCloud:
    """
    ROS message: sensor_msgs/PointCloud
    Generated by hakoniwa-ros2pdu.
    """

    header: Header
    points: List[Point32]
    channels: List[ChannelFloat32]

    def __init__(self):
        self.header = Header()
        self.points = []
        self.channels = []

    def __str__(self):
        return f"PointCloud(" + ", ".join([
            f"header={self.header}"
            f"points={self.points}"
            f"channels={self.channels}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()

    def to_dict(self):
        """
        Convert the object to a dictionary.
        """
        d = {}
        # handle field 'header'
        field_val = self.header
        if isinstance(field_val, bytearray):
            d['header'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['header'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['header'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['header'] = field_val
        # handle field 'points'
        field_val = self.points
        if isinstance(field_val, bytearray):
            d['points'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['points'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['points'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['points'] = field_val
        # handle field 'channels'
        field_val = self.channels
        if isinstance(field_val, bytearray):
            d['channels'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['channels'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['channels'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['channels'] = field_val
        return d

    @classmethod
    def from_dict(cls, d: dict):
        """
        Create an object from a dictionary.
        """
        obj = cls()
        # handle field 'header'
        if 'header' in d:
            field_type = cls.__annotations__.get('header')
            value = d['header']
            
            if field_type is bytearray:
                obj.header = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.header = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.header = value
            elif hasattr(field_type, 'from_dict'):
                obj.header = field_type.from_dict(value)
            else:
                obj.header = value
        # handle field 'points'
        if 'points' in d:
            field_type = cls.__annotations__.get('points')
            value = d['points']
            
            if field_type is bytearray:
                obj.points = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.points = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.points = value
            elif hasattr(field_type, 'from_dict'):
                obj.points = field_type.from_dict(value)
            else:
                obj.points = value
        # handle field 'channels'
        if 'channels' in d:
            field_type = cls.__annotations__.get('channels')
            value = d['channels']
            
            if field_type is bytearray:
                obj.channels = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.channels = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.channels = value
            elif hasattr(field_type, 'from_dict'):
                obj.channels = field_type.from_dict(value)
            else:
                obj.channels = value
        return obj

    def to_json(self, indent=2):
        """
        Convert the object to a JSON string.
        """
        import json
        return json.dumps(self.to_dict(), indent=indent)

    @classmethod
    def from_json(cls, json_str: str):
        """
        Create an object from a JSON string.
        """
        import json
        return cls.from_dict(json.loads(json_str))
