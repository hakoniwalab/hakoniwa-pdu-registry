defmodule HakoPdu.Ev3Msgs.Ev3PduSensor do
  @moduledoc """
  ROS message: ev3_msgs/Ev3PduSensor

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    head: HakoPdu.Ev3Msgs.Ev3PduSensorHeader.new(),

    buttons: [],

    color_sensors: [],

    touch_sensors: [],

    motor_angle: [],

    gyro_degree: 0,

    gyro_degree_rate: 0,

    sensor_ultrasonic: 0,

    gps_lat: 0.0,

    gps_lon: 0.0

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "head" => encode_value(msg.head),

      "buttons" => encode_value(msg.buttons),

      "color_sensors" => encode_value(msg.color_sensors),

      "touch_sensors" => encode_value(msg.touch_sensors),

      "motor_angle" => encode_value(msg.motor_angle),

      "gyro_degree" => encode_value(msg.gyro_degree),

      "gyro_degree_rate" => encode_value(msg.gyro_degree_rate),

      "sensor_ultrasonic" => encode_value(msg.sensor_ultrasonic),

      "gps_lat" => encode_value(msg.gps_lat),

      "gps_lon" => encode_value(msg.gps_lon)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      head: map |> get_value(:head, "head", HakoPdu.Ev3Msgs.Ev3PduSensorHeader.new()) |> decode_head(),

      buttons: map |> get_value(:buttons, "buttons", []) |> decode_buttons(),

      color_sensors: map |> get_value(:color_sensors, "color_sensors", []) |> decode_color_sensors(),

      touch_sensors: map |> get_value(:touch_sensors, "touch_sensors", []) |> decode_touch_sensors(),

      motor_angle: map |> get_value(:motor_angle, "motor_angle", []) |> decode_motor_angle(),

      gyro_degree: map |> get_value(:gyro_degree, "gyro_degree", 0) |> decode_gyro_degree(),

      gyro_degree_rate: map |> get_value(:gyro_degree_rate, "gyro_degree_rate", 0) |> decode_gyro_degree_rate(),

      sensor_ultrasonic: map |> get_value(:sensor_ultrasonic, "sensor_ultrasonic", 0) |> decode_sensor_ultrasonic(),

      gps_lat: map |> get_value(:gps_lat, "gps_lat", 0.0) |> decode_gps_lat(),

      gps_lon: map |> get_value(:gps_lon, "gps_lon", 0.0) |> decode_gps_lon()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_head(value) do
    if is_map(value), do: HakoPdu.Ev3Msgs.Ev3PduSensorHeader.from_map(value), else: value
  end


  defp decode_buttons(value) do
    value
  end


  defp decode_color_sensors(value) do
    Enum.map(value || [], &HakoPdu.Ev3Msgs.Ev3PduColorSensor.from_map/1)
  end


  defp decode_touch_sensors(value) do
    Enum.map(value || [], &HakoPdu.Ev3Msgs.Ev3PduTouchSensor.from_map/1)
  end


  defp decode_motor_angle(value) do
    value
  end


  defp decode_gyro_degree(value) do
    value
  end


  defp decode_gyro_degree_rate(value) do
    value
  end


  defp decode_sensor_ultrasonic(value) do
    value
  end


  defp decode_gps_lat(value) do
    value
  end


  defp decode_gps_lon(value) do
    value
  end

end
