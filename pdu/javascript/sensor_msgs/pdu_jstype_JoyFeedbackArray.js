import { JoyFeedback } from '../sensor_msgs/pdu_jstype_JoyFeedback.js';


/**
 * ROS message: sensor_msgs/JoyFeedbackArray
 * Generated by hakoniwa-ros2pdu.
 */
export class JoyFeedbackArray {
/** @type { Array<JoyFeedback> } */
    array;


    constructor() {
        this.array = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'array'
            const field_val = this.array;
            if (typeof field_val?.toDict === 'function') {
                d['array'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['array'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['array'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { JoyFeedbackArray }
     */
    static fromDict(d) {
        const obj = new JoyFeedbackArray();
        if (d.hasOwnProperty('array')) {
            const item_class = JoyFeedback;
            if (Array.isArray(d.array)) {
                obj.array = d.array.map(item => item_class.fromDict(item));
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { JoyFeedbackArray }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
