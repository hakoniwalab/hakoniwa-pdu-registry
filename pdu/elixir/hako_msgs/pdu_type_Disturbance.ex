defmodule HakoPdu.HakoMsgs.Disturbance do
  @moduledoc """
  ROS message: hako_msgs/Disturbance

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    d_temp: HakoPdu.HakoMsgs.DisturbanceTemperature.new(),

    d_wind: HakoPdu.HakoMsgs.DisturbanceWind.new(),

    d_atm: HakoPdu.HakoMsgs.DisturbanceAtm.new(),

    d_boundary: HakoPdu.HakoMsgs.DisturbanceBoundary.new(),

    d_user_custom: []

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "d_temp" => encode_value(msg.d_temp),

      "d_wind" => encode_value(msg.d_wind),

      "d_atm" => encode_value(msg.d_atm),

      "d_boundary" => encode_value(msg.d_boundary),

      "d_user_custom" => encode_value(msg.d_user_custom)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      d_temp: map |> get_value(:d_temp, "d_temp", HakoPdu.HakoMsgs.DisturbanceTemperature.new()) |> decode_d_temp(),

      d_wind: map |> get_value(:d_wind, "d_wind", HakoPdu.HakoMsgs.DisturbanceWind.new()) |> decode_d_wind(),

      d_atm: map |> get_value(:d_atm, "d_atm", HakoPdu.HakoMsgs.DisturbanceAtm.new()) |> decode_d_atm(),

      d_boundary: map |> get_value(:d_boundary, "d_boundary", HakoPdu.HakoMsgs.DisturbanceBoundary.new()) |> decode_d_boundary(),

      d_user_custom: map |> get_value(:d_user_custom, "d_user_custom", []) |> decode_d_user_custom()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_d_temp(value) do
    if is_map(value), do: HakoPdu.HakoMsgs.DisturbanceTemperature.from_map(value), else: value
  end


  defp decode_d_wind(value) do
    if is_map(value), do: HakoPdu.HakoMsgs.DisturbanceWind.from_map(value), else: value
  end


  defp decode_d_atm(value) do
    if is_map(value), do: HakoPdu.HakoMsgs.DisturbanceAtm.from_map(value), else: value
  end


  defp decode_d_boundary(value) do
    if is_map(value), do: HakoPdu.HakoMsgs.DisturbanceBoundary.from_map(value), else: value
  end


  defp decode_d_user_custom(value) do
    Enum.map(value || [], &HakoPdu.HakoMsgs.DisturbanceUserCustom.from_map/1)
  end

end
