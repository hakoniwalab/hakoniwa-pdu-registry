

/**
 * ROS message: sample_action_msgs/FibonacciResult
 * Generated by hakoniwa-ros2pdu.
 */
export class FibonacciResult {
/** @type { Array<number> } */
    sequence;


    constructor() {
        this.sequence = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'sequence'
            const field_val = this.sequence;
            if (typeof field_val?.toDict === 'function') {
                d['sequence'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['sequence'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['sequence'] = field_val.toString();
            } else {
                d['sequence'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { FibonacciResult }
     */
    static fromDict(d) {
        const obj = new FibonacciResult();
        if (d.hasOwnProperty('sequence')) {
            obj.sequence = d.sequence;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { FibonacciResult }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
