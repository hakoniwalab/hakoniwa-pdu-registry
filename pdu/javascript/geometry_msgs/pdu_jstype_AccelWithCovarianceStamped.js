import { Accel } from '../geometry_msgs/pdu_jstype_Accel.js';
import { AccelWithCovariance } from '../geometry_msgs/pdu_jstype_AccelWithCovariance.js';
import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';
import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: geometry_msgs/AccelWithCovarianceStamped
 * Generated by hakoniwa-ros2pdu.
 */
export class AccelWithCovarianceStamped {
/** @type { Header } */
    header;
/** @type { AccelWithCovariance } */
    accel;


    constructor() {
        this.header = new Header();
        this.accel = new AccelWithCovariance();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'accel'
            const field_val = this.accel;
            if (typeof field_val?.toDict === 'function') {
                d['accel'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['accel'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['accel'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { AccelWithCovarianceStamped }
     */
    static fromDict(d) {
        const obj = new AccelWithCovarianceStamped();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('accel')) {
            const field_class = AccelWithCovariance;
            if (d.accel) {
                obj.accel = field_class.fromDict(d.accel);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { AccelWithCovarianceStamped }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
