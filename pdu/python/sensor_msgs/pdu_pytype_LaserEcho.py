from typing import List

# dependencies for the generated Python class


# class definition for the ROS message
class LaserEcho:
    """
    ROS message: sensor_msgs/LaserEcho
    Generated by hakoniwa-ros2pdu.
    """

    echoes: List[float]

    def __init__(self):
        self.echoes = []

    def __str__(self):
        return f"LaserEcho(" + ", ".join([
            f"echoes={self.echoes}"
        ]) + ")"

    def __repr__(self):
        return self.__str__()

    def to_dict(self):
        """
        Convert the object to a dictionary.
        """
        d = {}
        # handle field 'echoes'
        field_val = self.echoes
        if isinstance(field_val, bytearray):
            d['echoes'] = list(field_val)
        elif hasattr(field_val, 'to_dict'):
            d['echoes'] = field_val.to_dict()
        elif isinstance(field_val, list):
            d['echoes'] = [item.to_dict() if hasattr(item, 'to_dict') else item for item in field_val]
        else:
            d['echoes'] = field_val
        return d

    @classmethod
    def from_dict(cls, d: dict):
        """
        Create an object from a dictionary.
        """
        obj = cls()
        # handle field 'echoes'
        if 'echoes' in d:
            field_type = cls.__annotations__.get('echoes')
            value = d['echoes']
            
            if field_type is bytearray:
                obj.echoes = bytearray(value)
            elif hasattr(field_type, '__origin__') and field_type.__origin__ is list:
                list_item_type = field_type.__args__[0]
                if hasattr(list_item_type, 'from_dict'):
                    obj.echoes = [list_item_type.from_dict(item) for item in value]
                else:
                    obj.echoes = value
            elif hasattr(field_type, 'from_dict'):
                obj.echoes = field_type.from_dict(value)
            else:
                obj.echoes = value
        return obj

    def to_json(self, indent=2):
        """
        Convert the object to a JSON string.
        """
        import json
        return json.dumps(self.to_dict(), indent=indent)

    @classmethod
    def from_json(cls, json_str: str):
        """
        Create an object from a JSON string.
        """
        import json
        return cls.from_dict(json.loads(json_str))
