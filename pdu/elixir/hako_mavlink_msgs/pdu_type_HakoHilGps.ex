defmodule HakoPdu.HakoMavlinkMsgs.HakoHilGps do
  @moduledoc """
  ROS message: hako_mavlink_msgs/HakoHilGps

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    time_usec: 0,

    lat: 0,

    lon: 0,

    alt: 0,

    eph: 0,

    epv: 0,

    vel: 0,

    vn: 0,

    ve: 0,

    vd: 0,

    cog: 0,

    satellites_visible: 0,

    id: 0,

    yaw: 0,

    fix_type: 0

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "time_usec" => encode_value(msg.time_usec),

      "lat" => encode_value(msg.lat),

      "lon" => encode_value(msg.lon),

      "alt" => encode_value(msg.alt),

      "eph" => encode_value(msg.eph),

      "epv" => encode_value(msg.epv),

      "vel" => encode_value(msg.vel),

      "vn" => encode_value(msg.vn),

      "ve" => encode_value(msg.ve),

      "vd" => encode_value(msg.vd),

      "cog" => encode_value(msg.cog),

      "satellites_visible" => encode_value(msg.satellites_visible),

      "id" => encode_value(msg.id),

      "yaw" => encode_value(msg.yaw),

      "fix_type" => encode_value(msg.fix_type)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      time_usec: map |> get_value(:time_usec, "time_usec", 0) |> decode_time_usec(),

      lat: map |> get_value(:lat, "lat", 0) |> decode_lat(),

      lon: map |> get_value(:lon, "lon", 0) |> decode_lon(),

      alt: map |> get_value(:alt, "alt", 0) |> decode_alt(),

      eph: map |> get_value(:eph, "eph", 0) |> decode_eph(),

      epv: map |> get_value(:epv, "epv", 0) |> decode_epv(),

      vel: map |> get_value(:vel, "vel", 0) |> decode_vel(),

      vn: map |> get_value(:vn, "vn", 0) |> decode_vn(),

      ve: map |> get_value(:ve, "ve", 0) |> decode_ve(),

      vd: map |> get_value(:vd, "vd", 0) |> decode_vd(),

      cog: map |> get_value(:cog, "cog", 0) |> decode_cog(),

      satellites_visible: map |> get_value(:satellites_visible, "satellites_visible", 0) |> decode_satellites_visible(),

      id: map |> get_value(:id, "id", 0) |> decode_id(),

      yaw: map |> get_value(:yaw, "yaw", 0) |> decode_yaw(),

      fix_type: map |> get_value(:fix_type, "fix_type", 0) |> decode_fix_type()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_time_usec(value) do
    value
  end


  defp decode_lat(value) do
    value
  end


  defp decode_lon(value) do
    value
  end


  defp decode_alt(value) do
    value
  end


  defp decode_eph(value) do
    value
  end


  defp decode_epv(value) do
    value
  end


  defp decode_vel(value) do
    value
  end


  defp decode_vn(value) do
    value
  end


  defp decode_ve(value) do
    value
  end


  defp decode_vd(value) do
    value
  end


  defp decode_cog(value) do
    value
  end


  defp decode_satellites_visible(value) do
    value
  end


  defp decode_id(value) do
    value
  end


  defp decode_yaw(value) do
    value
  end


  defp decode_fix_type(value) do
    value
  end

end
