

/**
 * ROS message: sensor_msgs/JoyFeedback
 * Generated by hakoniwa-ros2pdu.
 */
export class JoyFeedback {
/** @type { number } */
    type;
/** @type { number } */
    id;
/** @type { number } */
    intensity;


    constructor() {
        this.type = 0;
        this.id = 0;
        this.intensity = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'type'
            const field_val = this.type;
            if (typeof field_val?.toDict === 'function') {
                d['type'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['type'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['type'] = field_val;
            }
        }
        {
            // handle field 'id'
            const field_val = this.id;
            if (typeof field_val?.toDict === 'function') {
                d['id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['id'] = field_val;
            }
        }
        {
            // handle field 'intensity'
            const field_val = this.intensity;
            if (typeof field_val?.toDict === 'function') {
                d['intensity'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['intensity'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['intensity'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { JoyFeedback }
     */
    static fromDict(d) {
        const obj = new JoyFeedback();
        if (d.hasOwnProperty('type')) {
            obj.type = d.type;
        }
        if (d.hasOwnProperty('id')) {
            obj.id = d.id;
        }
        if (d.hasOwnProperty('intensity')) {
            obj.intensity = d.intensity;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { JoyFeedback }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
