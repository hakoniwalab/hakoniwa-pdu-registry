import { DroneVisualState } from '../hako_msgs/pdu_jstype_DroneVisualState.js';


/**
 * ROS message: hako_msgs/DroneVisualStateArray
 * Generated by hakoniwa-ros2pdu.
 */
export class DroneVisualStateArray {
/** @type { number } */
    sequence_id;
/** @type { number } */
    chunk_index;
/** @type { number } */
    chunk_count;
/** @type { number } */
    start_index;
/** @type { number } */
    valid_count;
/** @type { Array<DroneVisualState> } */
    drones;


    constructor() {
        this.sequence_id = 0;
        this.chunk_index = 0;
        this.chunk_count = 0;
        this.start_index = 0;
        this.valid_count = 0;
        this.drones = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'sequence_id'
            const field_val = this.sequence_id;
            if (typeof field_val?.toDict === 'function') {
                d['sequence_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['sequence_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['sequence_id'] = field_val;
            }
        }
        {
            // handle field 'chunk_index'
            const field_val = this.chunk_index;
            if (typeof field_val?.toDict === 'function') {
                d['chunk_index'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['chunk_index'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['chunk_index'] = field_val;
            }
        }
        {
            // handle field 'chunk_count'
            const field_val = this.chunk_count;
            if (typeof field_val?.toDict === 'function') {
                d['chunk_count'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['chunk_count'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['chunk_count'] = field_val;
            }
        }
        {
            // handle field 'start_index'
            const field_val = this.start_index;
            if (typeof field_val?.toDict === 'function') {
                d['start_index'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['start_index'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['start_index'] = field_val;
            }
        }
        {
            // handle field 'valid_count'
            const field_val = this.valid_count;
            if (typeof field_val?.toDict === 'function') {
                d['valid_count'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['valid_count'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['valid_count'] = field_val;
            }
        }
        {
            // handle field 'drones'
            const field_val = this.drones;
            if (typeof field_val?.toDict === 'function') {
                d['drones'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['drones'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['drones'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { DroneVisualStateArray }
     */
    static fromDict(d) {
        const obj = new DroneVisualStateArray();
        if (d.hasOwnProperty('sequence_id')) {
            obj.sequence_id = d.sequence_id;
        }
        if (d.hasOwnProperty('chunk_index')) {
            obj.chunk_index = d.chunk_index;
        }
        if (d.hasOwnProperty('chunk_count')) {
            obj.chunk_count = d.chunk_count;
        }
        if (d.hasOwnProperty('start_index')) {
            obj.start_index = d.start_index;
        }
        if (d.hasOwnProperty('valid_count')) {
            obj.valid_count = d.valid_count;
        }
        if (d.hasOwnProperty('drones')) {
            const item_class = DroneVisualState;
            if (Array.isArray(d.drones)) {
                obj.drones = d.drones.map(item => item_class.fromDict(item));
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { DroneVisualStateArray }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
