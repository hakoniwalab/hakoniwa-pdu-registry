import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';
import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: geometry_msgs/Vector3Stamped
 * Generated by hakoniwa-ros2pdu.
 */
export class Vector3Stamped {
/** @type { Header } */
    header;
/** @type { Vector3 } */
    vector;


    constructor() {
        this.header = new Header();
        this.vector = new Vector3();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'vector'
            const field_val = this.vector;
            if (typeof field_val?.toDict === 'function') {
                d['vector'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['vector'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['vector'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Vector3Stamped }
     */
    static fromDict(d) {
        const obj = new Vector3Stamped();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('vector')) {
            const field_class = Vector3;
            if (d.vector) {
                obj.vector = field_class.fromDict(d.vector);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Vector3Stamped }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
