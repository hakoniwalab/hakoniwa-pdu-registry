import { GetWorldTimeRequest } from '../hako_srv_msgs/pdu_jstype_GetWorldTimeRequest.js';
import { ServiceRequestHeader } from '../hako_srv_msgs/pdu_jstype_ServiceRequestHeader.js';


/**
 * ROS message: hako_srv_msgs/GetWorldTimeRequestPacket
 * Generated by hakoniwa-ros2pdu.
 */
export class GetWorldTimeRequestPacket {
/** @type { ServiceRequestHeader } */
    header;
/** @type { GetWorldTimeRequest } */
    body;


    constructor() {
        this.header = new ServiceRequestHeader();
        this.body = new GetWorldTimeRequest();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['header'] = field_val.toString();
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'body'
            const field_val = this.body;
            if (typeof field_val?.toDict === 'function') {
                d['body'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['body'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['body'] = field_val.toString();
            } else {
                d['body'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { GetWorldTimeRequestPacket }
     */
    static fromDict(d) {
        const obj = new GetWorldTimeRequestPacket();
        if (d.hasOwnProperty('header')) {
            const field_class = ServiceRequestHeader;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('body')) {
            const field_class = GetWorldTimeRequest;
            if (d.body) {
                obj.body = field_class.fromDict(d.body);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { GetWorldTimeRequestPacket }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
