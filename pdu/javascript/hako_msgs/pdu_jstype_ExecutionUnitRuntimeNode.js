

/**
 * ROS message: hako_msgs/ExecutionUnitRuntimeNode
 * Generated by hakoniwa-ros2pdu.
 */
export class ExecutionUnitRuntimeNode {
/** @type { number } */
    node_id;


    constructor() {
        this.node_id = 0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'node_id'
            const field_val = this.node_id;
            if (typeof field_val?.toDict === 'function') {
                d['node_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['node_id'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['node_id'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ExecutionUnitRuntimeNode }
     */
    static fromDict(d) {
        const obj = new ExecutionUnitRuntimeNode();
        if (d.hasOwnProperty('node_id')) {
            obj.node_id = d.node_id;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ExecutionUnitRuntimeNode }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
