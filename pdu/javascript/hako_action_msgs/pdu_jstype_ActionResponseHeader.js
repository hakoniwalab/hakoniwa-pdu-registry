

/**
 * ROS message: hako_action_msgs/ActionResponseHeader
 * Generated by hakoniwa-ros2pdu.
 */
export class ActionResponseHeader {
/** @type { number } */
    version;
/** @type { number } */
    response_kind;
/** @type { number } */
    status;
/** @type { number } */
    reserved;
/** @type { Array<number> } */
    goal_id;


    constructor() {
        this.version = 0;
        this.response_kind = 0;
        this.status = 0;
        this.reserved = 0;
        this.goal_id = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'version'
            const field_val = this.version;
            if (typeof field_val?.toDict === 'function') {
                d['version'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['version'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['version'] = field_val.toString();
            } else {
                d['version'] = field_val;
            }
        }
        {
            // handle field 'response_kind'
            const field_val = this.response_kind;
            if (typeof field_val?.toDict === 'function') {
                d['response_kind'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['response_kind'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['response_kind'] = field_val.toString();
            } else {
                d['response_kind'] = field_val;
            }
        }
        {
            // handle field 'status'
            const field_val = this.status;
            if (typeof field_val?.toDict === 'function') {
                d['status'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['status'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['status'] = field_val.toString();
            } else {
                d['status'] = field_val;
            }
        }
        {
            // handle field 'reserved'
            const field_val = this.reserved;
            if (typeof field_val?.toDict === 'function') {
                d['reserved'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['reserved'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['reserved'] = field_val.toString();
            } else {
                d['reserved'] = field_val;
            }
        }
        {
            // handle field 'goal_id'
            const field_val = this.goal_id;
            if (typeof field_val?.toDict === 'function') {
                d['goal_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['goal_id'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['goal_id'] = field_val.toString();
            } else {
                d['goal_id'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { ActionResponseHeader }
     */
    static fromDict(d) {
        const obj = new ActionResponseHeader();
        if (d.hasOwnProperty('version')) {
            obj.version = d.version;
        }
        if (d.hasOwnProperty('response_kind')) {
            obj.response_kind = d.response_kind;
        }
        if (d.hasOwnProperty('status')) {
            obj.status = d.status;
        }
        if (d.hasOwnProperty('reserved')) {
            obj.reserved = d.reserved;
        }
        if (d.hasOwnProperty('goal_id')) {
            obj.goal_id = d.goal_id;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { ActionResponseHeader }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
