defmodule HakoPdu.HakoMsgs.ExecutionUnitRuntimeStatus do
  @moduledoc """
  ROS message: hako_msgs/ExecutionUnitRuntimeStatus

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    config_hash: 0,

    unit_count: 0,

    status: [],

    epoch: [],

    curr_owner_node_id: [],

    next_owner_node_id: []

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "config_hash" => encode_value(msg.config_hash),

      "unit_count" => encode_value(msg.unit_count),

      "status" => encode_value(msg.status),

      "epoch" => encode_value(msg.epoch),

      "curr_owner_node_id" => encode_value(msg.curr_owner_node_id),

      "next_owner_node_id" => encode_value(msg.next_owner_node_id)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      config_hash: map |> get_value(:config_hash, "config_hash", 0) |> decode_config_hash(),

      unit_count: map |> get_value(:unit_count, "unit_count", 0) |> decode_unit_count(),

      status: map |> get_value(:status, "status", []) |> decode_status(),

      epoch: map |> get_value(:epoch, "epoch", []) |> decode_epoch(),

      curr_owner_node_id: map |> get_value(:curr_owner_node_id, "curr_owner_node_id", []) |> decode_curr_owner_node_id(),

      next_owner_node_id: map |> get_value(:next_owner_node_id, "next_owner_node_id", []) |> decode_next_owner_node_id()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_config_hash(value) do
    value
  end


  defp decode_unit_count(value) do
    value
  end


  defp decode_status(value) do
    value
  end


  defp decode_epoch(value) do
    value
  end


  defp decode_curr_owner_node_id(value) do
    value
  end


  defp decode_next_owner_node_id(value) do
    value
  end

end
