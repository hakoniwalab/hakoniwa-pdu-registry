

/**
 * ROS message: ackermann_msgs/AckermannDrive
 * Generated by hakoniwa-ros2pdu.
 */
export class AckermannDrive {
/** @type { number } */
    steering_angle;
/** @type { number } */
    steering_angle_velocity;
/** @type { number } */
    speed;
/** @type { number } */
    acceleration;
/** @type { number } */
    jerk;


    constructor() {
        this.steering_angle = 0.0;
        this.steering_angle_velocity = 0.0;
        this.speed = 0.0;
        this.acceleration = 0.0;
        this.jerk = 0.0;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'steering_angle'
            const field_val = this.steering_angle;
            if (typeof field_val?.toDict === 'function') {
                d['steering_angle'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['steering_angle'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['steering_angle'] = field_val.toString();
            } else {
                d['steering_angle'] = field_val;
            }
        }
        {
            // handle field 'steering_angle_velocity'
            const field_val = this.steering_angle_velocity;
            if (typeof field_val?.toDict === 'function') {
                d['steering_angle_velocity'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['steering_angle_velocity'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['steering_angle_velocity'] = field_val.toString();
            } else {
                d['steering_angle_velocity'] = field_val;
            }
        }
        {
            // handle field 'speed'
            const field_val = this.speed;
            if (typeof field_val?.toDict === 'function') {
                d['speed'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['speed'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['speed'] = field_val.toString();
            } else {
                d['speed'] = field_val;
            }
        }
        {
            // handle field 'acceleration'
            const field_val = this.acceleration;
            if (typeof field_val?.toDict === 'function') {
                d['acceleration'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['acceleration'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['acceleration'] = field_val.toString();
            } else {
                d['acceleration'] = field_val;
            }
        }
        {
            // handle field 'jerk'
            const field_val = this.jerk;
            if (typeof field_val?.toDict === 'function') {
                d['jerk'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['jerk'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['jerk'] = field_val.toString();
            } else {
                d['jerk'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { AckermannDrive }
     */
    static fromDict(d) {
        const obj = new AckermannDrive();
        if (d.hasOwnProperty('steering_angle')) {
            obj.steering_angle = d.steering_angle;
        }
        if (d.hasOwnProperty('steering_angle_velocity')) {
            obj.steering_angle_velocity = d.steering_angle_velocity;
        }
        if (d.hasOwnProperty('speed')) {
            obj.speed = d.speed;
        }
        if (d.hasOwnProperty('acceleration')) {
            obj.acceleration = d.acceleration;
        }
        if (d.hasOwnProperty('jerk')) {
            obj.jerk = d.jerk;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { AckermannDrive }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
