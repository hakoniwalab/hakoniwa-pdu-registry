import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Inertia } from '../geometry_msgs/pdu_jstype_Inertia.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';
import { Vector3 } from '../geometry_msgs/pdu_jstype_Vector3.js';


/**
 * ROS message: geometry_msgs/InertiaStamped
 * Generated by hakoniwa-ros2pdu.
 */
export class InertiaStamped {
/** @type { Header } */
    header;
/** @type { Inertia } */
    inertia;


    constructor() {
        this.header = new Header();
        this.inertia = new Inertia();
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'inertia'
            const field_val = this.inertia;
            if (typeof field_val?.toDict === 'function') {
                d['inertia'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['inertia'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['inertia'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { InertiaStamped }
     */
    static fromDict(d) {
        const obj = new InertiaStamped();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('inertia')) {
            const field_class = Inertia;
            if (d.inertia) {
                obj.inertia = field_class.fromDict(d.inertia);
            }
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { InertiaStamped }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
