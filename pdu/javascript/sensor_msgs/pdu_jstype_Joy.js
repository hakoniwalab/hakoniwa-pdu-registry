import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: sensor_msgs/Joy
 * Generated by hakoniwa-ros2pdu.
 */
export class Joy {
/** @type { Header } */
    header;
/** @type { Array<number> } */
    axes;
/** @type { Array<number> } */
    buttons;


    constructor() {
        this.header = new Header();
        this.axes = [];
        this.buttons = [];
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'axes'
            const field_val = this.axes;
            if (typeof field_val?.toDict === 'function') {
                d['axes'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['axes'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['axes'] = field_val;
            }
        }
        {
            // handle field 'buttons'
            const field_val = this.buttons;
            if (typeof field_val?.toDict === 'function') {
                d['buttons'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['buttons'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['buttons'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { Joy }
     */
    static fromDict(d) {
        const obj = new Joy();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('axes')) {
            obj.axes = d.axes;
        }
        if (d.hasOwnProperty('buttons')) {
            obj.buttons = d.buttons;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { Joy }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
