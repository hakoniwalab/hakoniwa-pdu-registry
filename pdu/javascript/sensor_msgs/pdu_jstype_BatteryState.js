import { Header } from '../std_msgs/pdu_jstype_Header.js';
import { Time } from '../builtin_interfaces/pdu_jstype_Time.js';


/**
 * ROS message: sensor_msgs/BatteryState
 * Generated by hakoniwa-ros2pdu.
 */
export class BatteryState {
/** @type { Header } */
    header;
/** @type { number } */
    voltage;
/** @type { number } */
    temperature;
/** @type { number } */
    current;
/** @type { number } */
    charge;
/** @type { number } */
    capacity;
/** @type { number } */
    design_capacity;
/** @type { number } */
    percentage;
/** @type { number } */
    power_supply_status;
/** @type { number } */
    power_supply_health;
/** @type { number } */
    power_supply_technology;
/** @type { boolean } */
    present;
/** @type { Array<number> } */
    cell_voltage;
/** @type { Array<number> } */
    cell_temperature;
/** @type { string } */
    location;
/** @type { string } */
    serial_number;


    constructor() {
        this.header = new Header();
        this.voltage = 0.0;
        this.temperature = 0.0;
        this.current = 0.0;
        this.charge = 0.0;
        this.capacity = 0.0;
        this.design_capacity = 0.0;
        this.percentage = 0.0;
        this.power_supply_status = 0;
        this.power_supply_health = 0;
        this.power_supply_technology = 0;
        this.present = false;
        this.cell_voltage = [];
        this.cell_temperature = [];
        this.location = "";
        this.serial_number = "";
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'header'
            const field_val = this.header;
            if (typeof field_val?.toDict === 'function') {
                d['header'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['header'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['header'] = field_val;
            }
        }
        {
            // handle field 'voltage'
            const field_val = this.voltage;
            if (typeof field_val?.toDict === 'function') {
                d['voltage'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['voltage'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['voltage'] = field_val;
            }
        }
        {
            // handle field 'temperature'
            const field_val = this.temperature;
            if (typeof field_val?.toDict === 'function') {
                d['temperature'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['temperature'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['temperature'] = field_val;
            }
        }
        {
            // handle field 'current'
            const field_val = this.current;
            if (typeof field_val?.toDict === 'function') {
                d['current'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['current'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['current'] = field_val;
            }
        }
        {
            // handle field 'charge'
            const field_val = this.charge;
            if (typeof field_val?.toDict === 'function') {
                d['charge'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['charge'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['charge'] = field_val;
            }
        }
        {
            // handle field 'capacity'
            const field_val = this.capacity;
            if (typeof field_val?.toDict === 'function') {
                d['capacity'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['capacity'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['capacity'] = field_val;
            }
        }
        {
            // handle field 'design_capacity'
            const field_val = this.design_capacity;
            if (typeof field_val?.toDict === 'function') {
                d['design_capacity'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['design_capacity'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['design_capacity'] = field_val;
            }
        }
        {
            // handle field 'percentage'
            const field_val = this.percentage;
            if (typeof field_val?.toDict === 'function') {
                d['percentage'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['percentage'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['percentage'] = field_val;
            }
        }
        {
            // handle field 'power_supply_status'
            const field_val = this.power_supply_status;
            if (typeof field_val?.toDict === 'function') {
                d['power_supply_status'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['power_supply_status'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['power_supply_status'] = field_val;
            }
        }
        {
            // handle field 'power_supply_health'
            const field_val = this.power_supply_health;
            if (typeof field_val?.toDict === 'function') {
                d['power_supply_health'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['power_supply_health'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['power_supply_health'] = field_val;
            }
        }
        {
            // handle field 'power_supply_technology'
            const field_val = this.power_supply_technology;
            if (typeof field_val?.toDict === 'function') {
                d['power_supply_technology'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['power_supply_technology'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['power_supply_technology'] = field_val;
            }
        }
        {
            // handle field 'present'
            const field_val = this.present;
            if (typeof field_val?.toDict === 'function') {
                d['present'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['present'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['present'] = field_val;
            }
        }
        {
            // handle field 'cell_voltage'
            const field_val = this.cell_voltage;
            if (typeof field_val?.toDict === 'function') {
                d['cell_voltage'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['cell_voltage'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['cell_voltage'] = field_val;
            }
        }
        {
            // handle field 'cell_temperature'
            const field_val = this.cell_temperature;
            if (typeof field_val?.toDict === 'function') {
                d['cell_temperature'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['cell_temperature'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['cell_temperature'] = field_val;
            }
        }
        {
            // handle field 'location'
            const field_val = this.location;
            if (typeof field_val?.toDict === 'function') {
                d['location'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['location'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['location'] = field_val;
            }
        }
        {
            // handle field 'serial_number'
            const field_val = this.serial_number;
            if (typeof field_val?.toDict === 'function') {
                d['serial_number'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['serial_number'] = field_val.map(item => typeof item?.toDict === 'function' ? item.toDict() : item);
            } else {
                d['serial_number'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { BatteryState }
     */
    static fromDict(d) {
        const obj = new BatteryState();
        if (d.hasOwnProperty('header')) {
            const field_class = Header;
            if (d.header) {
                obj.header = field_class.fromDict(d.header);
            }
        }
        if (d.hasOwnProperty('voltage')) {
            obj.voltage = d.voltage;
        }
        if (d.hasOwnProperty('temperature')) {
            obj.temperature = d.temperature;
        }
        if (d.hasOwnProperty('current')) {
            obj.current = d.current;
        }
        if (d.hasOwnProperty('charge')) {
            obj.charge = d.charge;
        }
        if (d.hasOwnProperty('capacity')) {
            obj.capacity = d.capacity;
        }
        if (d.hasOwnProperty('design_capacity')) {
            obj.design_capacity = d.design_capacity;
        }
        if (d.hasOwnProperty('percentage')) {
            obj.percentage = d.percentage;
        }
        if (d.hasOwnProperty('power_supply_status')) {
            obj.power_supply_status = d.power_supply_status;
        }
        if (d.hasOwnProperty('power_supply_health')) {
            obj.power_supply_health = d.power_supply_health;
        }
        if (d.hasOwnProperty('power_supply_technology')) {
            obj.power_supply_technology = d.power_supply_technology;
        }
        if (d.hasOwnProperty('present')) {
            obj.present = d.present;
        }
        if (d.hasOwnProperty('cell_voltage')) {
            obj.cell_voltage = d.cell_voltage;
        }
        if (d.hasOwnProperty('cell_temperature')) {
            obj.cell_temperature = d.cell_temperature;
        }
        if (d.hasOwnProperty('location')) {
            obj.location = d.location;
        }
        if (d.hasOwnProperty('serial_number')) {
            obj.serial_number = d.serial_number;
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { BatteryState }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
