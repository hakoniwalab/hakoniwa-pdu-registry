import { LogicalTime } from '../hako_srv_msgs/pdu_jstype_LogicalTime.js';


/**
 * ROS message: hako_srv_msgs/AttachResponse
 * Generated by hakoniwa-ros2pdu.
 */
export class AttachResponse {
/** @type { number } */
    result_code;
/** @type { bigint } */
    session_id;
/** @type { LogicalTime } */
    world_time;
/** @type { bigint } */
    delta_asset_tick;


    constructor() {
        this.result_code = 0;
        this.session_id = 0n;
        this.world_time = new LogicalTime();
        this.delta_asset_tick = 0n;
    }

    /**
     * @returns {object}
     */
    toDict() {
        const d = {};
        {
            // handle field 'result_code'
            const field_val = this.result_code;
            if (typeof field_val?.toDict === 'function') {
                d['result_code'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['result_code'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['result_code'] = field_val.toString();
            } else {
                d['result_code'] = field_val;
            }
        }
        {
            // handle field 'session_id'
            const field_val = this.session_id;
            if (typeof field_val?.toDict === 'function') {
                d['session_id'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['session_id'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['session_id'] = field_val.toString();
            } else {
                d['session_id'] = field_val;
            }
        }
        {
            // handle field 'world_time'
            const field_val = this.world_time;
            if (typeof field_val?.toDict === 'function') {
                d['world_time'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['world_time'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['world_time'] = field_val.toString();
            } else {
                d['world_time'] = field_val;
            }
        }
        {
            // handle field 'delta_asset_tick'
            const field_val = this.delta_asset_tick;
            if (typeof field_val?.toDict === 'function') {
                d['delta_asset_tick'] = field_val.toDict();
            } else if (Array.isArray(field_val)) {
                d['delta_asset_tick'] = field_val.map(item => {
                    if (typeof item?.toDict === 'function') {
                        return item.toDict();
                    }
                    if (typeof item === 'bigint') {
                        return item.toString();
                    }
                    return item;
                });
            } else if (typeof field_val === 'bigint') {
                d['delta_asset_tick'] = field_val.toString();
            } else {
                d['delta_asset_tick'] = field_val;
            }
        }
        return d;
    }

    /**
     * @param {object} d
     * @returns { AttachResponse }
     */
    static fromDict(d) {
        const obj = new AttachResponse();
        if (d.hasOwnProperty('result_code')) {
            obj.result_code = d.result_code;
        }
        if (d.hasOwnProperty('session_id')) {
            obj.session_id = BigInt(d.session_id);
        }
        if (d.hasOwnProperty('world_time')) {
            const field_class = LogicalTime;
            if (d.world_time) {
                obj.world_time = field_class.fromDict(d.world_time);
            }
        }
        if (d.hasOwnProperty('delta_asset_tick')) {
            obj.delta_asset_tick = BigInt(d.delta_asset_tick);
        }
        return obj;
    }

    /**
     * @returns {string}
     */
    toJSON(indent = 2) {
        return JSON.stringify(this.toDict(), null, indent);
    }

    /**
     * @param {string} json_str
     * @returns { AttachResponse }
     */
    static fromJSON(json_str) {
        return this.fromDict(JSON.parse(json_str));
    }
}
