defmodule HakoPdu.SensorMsgs.BatteryState do
  @moduledoc """
  ROS message: sensor_msgs/BatteryState

  Generated by hakoniwa-ros2pdu.
  """

  defstruct [

    header: HakoPdu.StdMsgs.Header.new(),

    voltage: 0.0,

    temperature: 0.0,

    current: 0.0,

    charge: 0.0,

    capacity: 0.0,

    design_capacity: 0.0,

    percentage: 0.0,

    power_supply_status: 0,

    power_supply_health: 0,

    power_supply_technology: 0,

    present: false,

    cell_voltage: [],

    cell_temperature: [],

    location: "",

    serial_number: ""

  ]

  def new(attrs \\ %{}) when is_map(attrs) do
    struct(__MODULE__, attrs)
  end

  def to_map(%__MODULE__{} = msg) do
    %{

      "header" => encode_value(msg.header),

      "voltage" => encode_value(msg.voltage),

      "temperature" => encode_value(msg.temperature),

      "current" => encode_value(msg.current),

      "charge" => encode_value(msg.charge),

      "capacity" => encode_value(msg.capacity),

      "design_capacity" => encode_value(msg.design_capacity),

      "percentage" => encode_value(msg.percentage),

      "power_supply_status" => encode_value(msg.power_supply_status),

      "power_supply_health" => encode_value(msg.power_supply_health),

      "power_supply_technology" => encode_value(msg.power_supply_technology),

      "present" => encode_value(msg.present),

      "cell_voltage" => encode_value(msg.cell_voltage),

      "cell_temperature" => encode_value(msg.cell_temperature),

      "location" => encode_value(msg.location),

      "serial_number" => encode_value(msg.serial_number)

    }
  end

  def from_map(map) when is_map(map) do
    %__MODULE__{

      header: map |> get_value(:header, "header", HakoPdu.StdMsgs.Header.new()) |> decode_header(),

      voltage: map |> get_value(:voltage, "voltage", 0.0) |> decode_voltage(),

      temperature: map |> get_value(:temperature, "temperature", 0.0) |> decode_temperature(),

      current: map |> get_value(:current, "current", 0.0) |> decode_current(),

      charge: map |> get_value(:charge, "charge", 0.0) |> decode_charge(),

      capacity: map |> get_value(:capacity, "capacity", 0.0) |> decode_capacity(),

      design_capacity: map |> get_value(:design_capacity, "design_capacity", 0.0) |> decode_design_capacity(),

      percentage: map |> get_value(:percentage, "percentage", 0.0) |> decode_percentage(),

      power_supply_status: map |> get_value(:power_supply_status, "power_supply_status", 0) |> decode_power_supply_status(),

      power_supply_health: map |> get_value(:power_supply_health, "power_supply_health", 0) |> decode_power_supply_health(),

      power_supply_technology: map |> get_value(:power_supply_technology, "power_supply_technology", 0) |> decode_power_supply_technology(),

      present: map |> get_value(:present, "present", false) |> decode_present(),

      cell_voltage: map |> get_value(:cell_voltage, "cell_voltage", []) |> decode_cell_voltage(),

      cell_temperature: map |> get_value(:cell_temperature, "cell_temperature", []) |> decode_cell_temperature(),

      location: map |> get_value(:location, "location", "") |> decode_location(),

      serial_number: map |> get_value(:serial_number, "serial_number", "") |> decode_serial_number()

    }
  end

  def to_json(%__MODULE__{} = msg) do
    msg
    |> to_map()
    |> json_encode()
  end

  def from_json(json) when is_binary(json) do
    json
    |> json_decode()
    |> from_map()
  end

  defp get_value(map, atom_key, string_key, default) do
    Map.get(map, atom_key, Map.get(map, string_key, default))
  end

  defp encode_value(values) when is_list(values), do: Enum.map(values, &encode_value/1)
  defp encode_value(%{__struct__: module} = value) do
    if function_exported?(module, :to_map, 1), do: module.to_map(value), else: value
  end
  defp encode_value(value), do: value

  defp json_encode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :encode!, 1) -> apply(Jason, :encode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :encode!, 1) -> apply(JSON, :encode!, [value])
      true -> raise "No JSON encoder found. Add Jason or JSON to use to_json/1."
    end
  end

  defp json_decode(value) do
    cond do
      Code.ensure_loaded?(Jason) and function_exported?(Jason, :decode!, 1) -> apply(Jason, :decode!, [value])
      Code.ensure_loaded?(JSON) and function_exported?(JSON, :decode!, 1) -> apply(JSON, :decode!, [value])
      true -> raise "No JSON decoder found. Add Jason or JSON to use from_json/1."
    end
  end


  defp decode_header(value) do
    if is_map(value), do: HakoPdu.StdMsgs.Header.from_map(value), else: value
  end


  defp decode_voltage(value) do
    value
  end


  defp decode_temperature(value) do
    value
  end


  defp decode_current(value) do
    value
  end


  defp decode_charge(value) do
    value
  end


  defp decode_capacity(value) do
    value
  end


  defp decode_design_capacity(value) do
    value
  end


  defp decode_percentage(value) do
    value
  end


  defp decode_power_supply_status(value) do
    value
  end


  defp decode_power_supply_health(value) do
    value
  end


  defp decode_power_supply_technology(value) do
    value
  end


  defp decode_present(value) do
    value
  end


  defp decode_cell_voltage(value) do
    value
  end


  defp decode_cell_temperature(value) do
    value
  end


  defp decode_location(value) do
    value
  end


  defp decode_serial_number(value) do
    value
  end

end
